[tool.poetry]
name = "Pogorelov_webflask"
version = "0.1.0"
description = ""
authors = ["Yurii .p <stroitelisf@yandex.ru>"]

[tool.poetry.dependencies]
python = "^3.6"
flask = "^1.1.2"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
